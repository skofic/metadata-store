# `_type_string_IPv6`

**`_title`**

IPv6 Address

**`_definition`**

The scalar data type for IPv6 addresses conforming to the JSON Schema `ipv6` format.

**`_description`**

`_type_string_IPv6` declares that the value is an [IPv6 address](https://en.wikipedia.org/wiki/IPv6). A member of [`_type_data_comparable`](_type_data_comparable.md) and [`_type_data_dict_key`](_type_data_dict_key.md).

---

**`_code`**

```json
{
	"_nid": "_type_string",
	"_lid": "IPv6",
	"_gid": "_type_string_IPv6",
	"_aid": ["IPv6"]
}
```

**`_domn`**

```json
{
	"_term_type": ["_type_term_enum-item"]
}
```
