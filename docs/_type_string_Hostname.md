# `_type_string_Hostname`

**`_title`**

Hostname

**`_definition`**

The scalar data type for internet hostnames conforming to the JSON Schema `hostname` format.

**`_description`**

`_type_string_Hostname` declares that the value is an [internet hostname](https://en.wikipedia.org/wiki/Hostname). A member of [`_type_data_comparable`](_type_data_comparable.md) and [`_type_data_dict_key`](_type_data_dict_key.md).

---

**`_code`**

```json
{
  "_aid" : [
    "Hostname"
  ],
  "_gid" : "_type_string_Hostname",
  "_lid" : "Hostname",
  "_nid" : "_type_string"
}
```
